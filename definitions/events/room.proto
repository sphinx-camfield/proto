syntax = "proto3";
package sphinx.camfield.events;
option go_package = "github.com/sphinx-camfield/proto/pkg/events";

message DeviceAssigned {
  string device = 1;
  string room = 2;
}

message DeviceRemoved {
  string device = 1;
  string room = 2;
}
