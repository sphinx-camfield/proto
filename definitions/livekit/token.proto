syntax = "proto3";
package camfield.livekit.token;
option go_package = "github.com/sphinx/camfield/livekit-token-provider/livekit";

message Empty {}

message RoomTokenReply {
  string room = 1;
  string token = 2;
  string endpoint = 3;
  int64 expiry = 4;
}

service Token {
  rpc Request(Empty) returns (RoomTokenReply);
}
